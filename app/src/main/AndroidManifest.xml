<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.capstone">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_rencara"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_rencara_round"
        android:supportsRtl="true"
        android:theme="@style/SplashTheme">
        <activity android:name=".Customer.OrderDetails"></activity>
        <activity android:name=".Customer.ProductListActivity" />
        <activity android:name=".Customer.ProductDetailsActivity" />
        <activity android:name=".Customer.EditCustActivity" />
        <activity
            android:name=".Vendor.DashboardActivity"
            android:label="@string/title_activity_dashboard"
            android:theme="@style/SplashTheme.NoActionBar" />
        <activity
            android:name=".Customer.MainActivity"
            android:theme="@style/SplashTheme.NoActionBar" />
        <activity android:name=".SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpActivity" />
    </application>

</manifest>